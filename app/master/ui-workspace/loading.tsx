import ConsoleInstantLoading from '../../../components/console/ConsoleInstantLoading';

export default function MasterUiWorkspaceLoading() {
  return <ConsoleInstantLoading role="master" title="UI workspace." description="Loading the master visual governance shell while the workspace model prepares." datasetLabel="UI workspace model" />;
}
